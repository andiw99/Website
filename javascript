-Semicolon at the end of a line
-Variable delcaration with "var"
-.append = .push
-.pop removes only last element of list
-.shift removes first element of list
-.unshift = .append at the beginning of the array
-function declartion with "function(parameters) {...}" instead of def
-function contents in curly brackets {}
-if statements written like normal function: "if (a==b) {do this}"
-for statement written like: " for (init, condition, incremention/final expression) { do this }
-switch (varible) {
		case 1:
			do this;
		case 2:
			do that
		}
 acts like "	if (variable == 1) { do this }
		elif (variable == 2) { do that }"
-ternary operator: if (condition) ? do this if true : do that if false
-let: variablendeklaration: nur einmal möglich, limitiert auf block in dem definiert wurde (var global, funktional)
-const: wie let nur read-only
-mainly use let and const
-dictionarys werden objects genannt, deklaration mit var, keys genannt properties
-access der values mit object.key oder object["key"] 